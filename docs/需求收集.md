## 需求收集

6cc53a99-46af-4397-9b85-ff25414f485e 
1. 状态是排队 queued   sse就开始连接了  ; 应该是running状态 才应该开始sse ；
 查详情 需要一直查直到有结果 500 / 排队 queued  就一直查 ；

2. 虽然是sse是queued状态 但是实际上中柏的服务是在跑的  服务区一直在响  ； 但是过了一段时间有真在跑 ； 

- 2.1 添加排查 ： 1. tvpage请求成功后 需要打印 download下载路径   

-  2.2 添加定时任务  1. tv 半小时一跑 ，如果有任务运行中running 退出这次任务 ； 查询列表是否呀任务在等待 ， 没有则退出这次任务 ； 查询有等待任务 则执行 ； 当调用接口执行完成之后  执行这个任务
   

- 2.3 修改 ： tv转译成功之后  sse会立即断开  导致最后一两次的转译结果还没有通过sse传导到前端  直接结束了   修改 需要保证 sse 传回了完整的字段


## 3. tvpage 的状态显示没有了 


工具的开发  nginx 日志的暴露外网访问  当访问不到的时候 得确认连接到哪了   npx serve 开启多个占内存多吗 